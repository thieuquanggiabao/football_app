name: football_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  timeago: ^3.7.0
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.0
  intl: ^0.20.2
  google_sign_in: ^6.2.2
  url_launcher: ^6.3.2
  share_plus: ^12.0.2
  http: ^1.2.0
  shimmer: ^3.0.0 # Thêm thư viện hiệu ứng Skeleton/Shimmer

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
